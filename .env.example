# ── Development (local) ──────────────────────────────────
DATABASE_URL="file:./dev.db"
AUTH_SECRET="replace-with-openssl-rand-base64-32"
AUTH_URL="http://localhost:3000"

# ── Production (Plesk: https://sikilat.site) ─────────────
# Copy values below into:
#   1) file `.env` on the server (Application Root), AND/OR
#   2) Plesk → Node.js → Custom environment variables
#
# DATABASE_URL="file:./prod.db"
# AUTH_SECRET="paste-random-secret-here"
# AUTH_URL="https://sikilat.site"
# NODE_ENV="production"
